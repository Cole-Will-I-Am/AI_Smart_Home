PyYAML>=6.0,<7.0
pytest>=7.0,<9.0
# Optional — only needed for a LIVE Claude ops-layer run (pytest -m live).
# The offline tests and the simulator do not import it.
anthropic>=0.40,<1.0
# Optional — only needed for the live Home Assistant WebSocket event bridge
# (real-adapter REST commands are stdlib-only; the offline tests need neither).
websocket-client>=1.6,<2.0
